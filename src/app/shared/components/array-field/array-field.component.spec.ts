import { TestBed } from '@angular/core/testing';

import { ArrayFieldComponent } from './array-field.component';

describe('UiArrayFieldComponent', () => {
    // let component: UiArrayFieldComponent;
    // let fixture: ComponentFixture<UiArrayFieldComponent>;

    beforeEach(async () => {
        await TestBed.configureTestingModule({
            declarations: [ ArrayFieldComponent ]
        }).compileComponents();
    });

    // beforeEach(() => {
    //     fixture = TestBed.createComponent(UiArrayFieldComponent);
    //     component = fixture.componentInstance;
    //     fixture.detectChanges();
    // });

    // it('should create', () => {
    //     expect(component).toBeTruthy();
    // });
});
